interface:
  display_name: "QA"
  short_description: "QA setup, verification, explain, and proof videos"
  brand_color: "#145A5A"
  default_prompt: "Use $qa to verify this work with Browser Use truth and produce a proof video only after the gates pass."
policy:
  allow_implicit_invocation: true
